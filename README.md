# get_mobile

* The "dist" folder should be deployed on the web server. 
* The sql file should also be used to setup the database. 
* The configurations for the database connection can be found in dist/include/database_config.php
